@model QuanLyDiemRenLuyen.Models.UploadProofViewModel
@{
    ViewBag.Title = "Upload minh chứng";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .upload-container {
        max-width: 700px;
        margin: 0 auto;
        padding: 20px;
    }

    .back-link {
        display: inline-block;
        margin-bottom: 20px;
        color: #3498db;
        text-decoration: none;
    }

    .back-link:hover {
        text-decoration: underline;
    }

    .upload-card {
        background: white;
        border-radius: 8px;
        padding: 30px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .page-title {
        font-size: 24px;
        font-weight: 600;
        color: #2c3e50;
        margin-bottom: 10px;
    }

    .activity-name {
        color: #3498db;
        font-size: 18px;
        margin-bottom: 30px;
    }

    .form-group {
        margin-bottom: 25px;
    }

    .form-label {
        display: block;
        font-weight: 500;
        color: #555;
        margin-bottom: 8px;
    }

    .form-label.required::after {
        content: " *";
        color: #e74c3c;
    }

    .form-control {
        width: 100%;
        padding: 12px;
        border: 1px solid #ddd;
        border-radius: 4px;
        font-size: 14px;
    }

    .form-control:focus {
        outline: none;
        border-color: #3498db;
        box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.1);
    }

    .file-input-wrapper {
        position: relative;
        overflow: hidden;
        display: inline-block;
        width: 100%;
    }

    .file-input-wrapper input[type=file] {
        position: absolute;
        left: -9999px;
    }

    .file-input-label {
        display: block;
        padding: 12px;
        background: #f8f9fa;
        border: 2px dashed #ddd;
        border-radius: 4px;
        text-align: center;
        cursor: pointer;
        transition: all 0.3s;
    }

    .file-input-label:hover {
        background: #e9ecef;
        border-color: #3498db;
    }

    .file-name {
        margin-top: 10px;
        color: #666;
        font-size: 14px;
    }

    .help-text {
        font-size: 13px;
        color: #999;
        margin-top: 5px;
    }

    .current-proof {
        background: #f8f9fa;
        padding: 15px;
        border-radius: 6px;
        margin-bottom: 20px;
    }

    .badge {
        padding: 5px 12px;
        border-radius: 20px;
        font-size: 12px;
        font-weight: 500;
    }

    .badge-warning {
        background: #f39c12;
        color: white;
    }

    .badge-success {
        background: #27ae60;
        color: white;
    }

    .badge-danger {
        background: #e74c3c;
        color: white;
    }

    .btn-group {
        display: flex;
        gap: 15px;
        margin-top: 30px;
    }

    .btn {
        padding: 12px 30px;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        font-size: 16px;
        font-weight: 500;
        transition: all 0.3s;
    }

    .btn-primary {
        background: #3498db;
</style>

<div class="upload-container">
    <a href="@Url.Action("ActivityDetail", "Student", new { id = Model.ActivityId })" class="back-link">← Quay lại chi
        tiết hoạt động</a>

    @if (ViewBag.ErrorMessage != null)
    {
        <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
    }

    <div class="upload-card">
        <h1 class="page-title">📤 Upload minh chứng tham gia</h1>
        <p class="activity-name">@Model.ActivityTitle</p>

        @if (!string.IsNullOrEmpty(Model.CurrentProofFilePath))
        {
            <div class="current-proof">
                <h3>Minh chứng hiện tại</h3>
                <p>
                    <strong>Trạng thái:</strong>
                    @if (Model.CurrentProofStatus == "PENDING")
                    {
                        <span class="badge badge-warning">Chờ duyệt</span>
                    }
                    else if (Model.CurrentProofStatus == "APPROVED")
                    {
                        <span class="badge badge-success">Đã duyệt</span>
                    }
                    else if (Model.CurrentProofStatus == "REJECTED")
                    {
                        <span class="badge badge-danger">Bị từ chối</span>
                    }
                </p>
                <p><strong>Đã upload:</strong> @Model.CurrentProofUploadedAt?.ToString("dd/MM/yyyy HH:mm")</p>
                <p><small>Upload file mới sẽ thay thế file cũ</small></p>
            </div>
        }

        @using (Html.BeginForm("UploadProof", "Student", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            @Html.AntiForgeryToken()
            <input type="hidden" name="activityId" value="@Model.ActivityId" />

            <div class="form-group">
                <label class="form-label required">File minh chứng</label>
                <div class="file-input-wrapper">
                    <input type="file" name="proofFile" id="proofFile" accept=".jpg,.jpeg,.png,.pdf" required />
                    <label for="proofFile" class="file-input-label">
                        📁 Chọn file (JPG, PNG, PDF)
                    </label>
                </div>
                <div class="file-name" id="fileName"></div>
                <p class="help-text">Chấp nhận file ảnh (JPG, PNG) hoặc PDF. Kích thước tối đa: 5MB</p>
            </div>

            <div class="form-group">
                <label class="form-label">Ghi chú</label>
                <textarea name="note" class="form-control" rows="4" placeholder="Nhập ghi chú (nếu có)..."></textarea>
                <p class="help-text">Tối đa 500 ký tự</p>
            </div>

            <div class="btn-group">
                <button type="submit" class="btn btn-primary">
                    ✅ Upload minh chứng
                </button>
                <a href="@Url.Action("ActivityDetail", "Student", new { id = Model.ActivityId })" class="btn btn-secondary">
                    ❌ Hủy
                </a>
            </div>
        }
    </div>
</div>

<script>
    // Display selected file name
    document.getElementById('proofFile').addEventListener('change', function (e) {
        const fileName = e.target.files[0]?.name;
        const fileNameDisplay = document.getElementById('fileName');
        if (fileName) {
            fileNameDisplay.textContent = '✓ Đã chọn: ' + fileName;
            fileNameDisplay.style.color = '#27ae60';
        } else {
            fileNameDisplay.textContent = '';
        }
    });

    // Handle form submission
    document.querySelector('form').addEventListener('submit', function (e) {
        const fileInput = document.getElementById('proofFile');
        if (!fileInput.files || !fileInput.files[0]) {
            e.preventDefault();
            alert('Vui lòng chọn file minh chứng');
            return false;
        }

        const file = fileInput.files[0];
        const maxSize = 5 * 1024 * 1024; // 5MB

        if (file.size > maxSize) {
            e.preventDefault();
            alert('File không được vượt quá 5MB');
            return false;
        }

        const button = this.querySelector('button[type="submit"]');
        button.disabled = true;
        button.textContent = '⏳ Đang upload...';
    });
</script>

color: white;
flex: 1;
}

.btn-primary:hover {
background: #2980b9;
}

.btn-secondary {
background: #95a5a6;
color: white;
}

.btn-secondary:hover {
background: #7f8c8d;
}

.alert {
padding: 15px;
border-radius: 6px;
margin-bottom: 20px;
}

.alert-danger {
background: #f8d7da;
color: #721c24;
border: 1px solid #f5c6cb;
}