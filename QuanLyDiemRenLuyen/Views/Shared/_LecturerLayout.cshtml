<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Giảng Viên - Quản Lý Điểm Rèn Luyện</title>

    <!-- Google Fonts - Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
        rel="stylesheet">

    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
        integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #f8fafc;
            color: #0f172a;
            font-size: 14px;
        }

        /* Header */
        .admin-header {
            background: #0ea5e9; /* Light blue for Lecturer */
            color: white;
            padding: 16px 32px;
            border-bottom: 1px solid #e2e8f0;
            position: fixed;
            top: 0;
            left: 260px;
            right: 0;
            z-index: 999;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: left 0.3s ease;
        }

        .admin-header.sidebar-collapsed {
            left: 70px;
        }

        .admin-header h1 {
            font-size: 20px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .sidebar-toggle {
            background: transparent;
            border: none;
            color: white;
            font-size: 20px;
            cursor: pointer;
            padding: 8px;
            border-radius: 6px;
        }

        .sidebar-toggle:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        /* Sidebar */
        .admin-sidebar {
            position: fixed;
            left: 0;
            top: 0;
            bottom: 0;
            width: 260px;
            background: white;
            border-right: 1px solid #e2e8f0;
            display: flex;
            flex-direction: column;
            transition: width 0.3s ease;
            z-index: 1000;
        }

        .admin-sidebar.collapsed {
            width: 70px;
        }

        .sidebar-header {
            padding: 20px;
            border-bottom: 1px solid #e2e8f0;
            background: #f8fafc;
        }

        .brand {
            font-size: 18px;
            font-weight: 700;
            color: #0ea5e9;
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 8px 0;
            border-top: 1px solid #e2e8f0;
            margin-top: 8px;
        }

        .user-avatar {
            width: 32px;
            height: 32px;
            background: #e2e8f0;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #64748b;
            font-size: 14px;
        }

        .user-details {
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        .user-name {
            font-weight: 600;
            color: #0f172a;
            font-size: 14px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .user-role {
            font-size: 12px;
            color: #64748b;
        }

        .admin-sidebar.collapsed .brand span,
        .admin-sidebar.collapsed .user-details {
            display: none;
        }

        .admin-sidebar.collapsed .sidebar-header {
            padding: 20px 10px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .admin-sidebar.collapsed .user-info {
            border-top: none;
            margin-top: 12px;
            justify-content: center;
        }

        .sidebar-menu {
            flex: 1;
            overflow-y: auto;
            padding: 12px 0;
        }

        .nav-item {
            padding: 12px 24px;
            display: flex;
            align-items: center;
            gap: 12px;
            color: #64748b;
            text-decoration: none;
            border-left: 3px solid transparent;
            font-weight: 500;
            transition: all 0.2s;
        }

        .admin-sidebar.collapsed .nav-item {
            padding: 12px;
            justify-content: center;
        }

        .nav-item:hover {
            background: #f0f9ff;
            color: #0ea5e9;
        }

        .nav-item.active {
            background: #e0f2fe;
            color: #0ea5e9;
            border-left-color: #0ea5e9;
            font-weight: 600;
        }

        .nav-item i {
            width: 20px;
            text-align: center;
            flex-shrink: 0;
            font-size: 16px;
        }

        .admin-sidebar.collapsed .nav-item span {
            display: none;
        }

        .sidebar-footer {
            padding: 16px;
            border-top: 1px solid #e2e8f0;
        }

        .btn-logout {
            display: flex;
            align-items: center;
            gap: 12px;
            width: 100%;
            padding: 10px 16px;
            background: #fee2e2;
            color: #991b1b;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.2s;
        }

        .btn-logout:hover {
            background: #fecaca;
        }

        .admin-sidebar.collapsed .btn-logout {
            padding: 10px;
            justify-content: center;
        }

        .admin-sidebar.collapsed .btn-logout span {
            display: none;
        }

        /* Main Content */
        .admin-content {
            margin-left: 260px;
            margin-top: 60px;
            padding: 32px;
            min-height: calc(100vh - 60px);
            transition: margin-left 0.3s ease;
        }

        .admin-content.sidebar-collapsed {
            margin-left: 70px;
        }

        /* Alerts */
        .alert {
            padding: 16px 20px;
            border-radius: 8px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 12px;
            border: 1px solid;
        }

        .alert-success {
            background: #f0fdf4;
            color: #166534;
            border-color: #bbf7d0;
        }

        .alert-danger {
            background: #fef2f2;
            color: #991b1b;
            border-color: #fecaca;
        }

        .alert-info {
            background: #f0f9ff;
            color: #075985;
            border-color: #bae6fd;
        }
    </style>
</head>

<body>
    <!-- Header -->
    <div class="admin-header" id="adminHeader">
        <h1>
            <button class="sidebar-toggle" id="sidebarToggle">
                <i class="fas fa-bars"></i>
            </button>
            @ViewBag.Title
        </h1>
    </div>

    <!-- Sidebar -->
    <div class="admin-sidebar" id="adminSidebar">
        <div class="sidebar-header">
            <div class="brand">
                <i class="fas fa-chalkboard-teacher"></i>
                <span>GIẢNG VIÊN</span>
            </div>
            <div class="user-info">
                <div class="user-avatar">
                    <i class="fas fa-user"></i>
                </div>
                <div class="user-details">
                    <span class="user-name">@Session["FullName"]</span>
                    <span class="user-role">Lecturer</span>
                </div>
            </div>
        </div>

        <div class="sidebar-menu">
            <a href="@Url.Action("Index", "Dashboard", new { area = "" })"
                class="nav-item @(ViewContext.RouteData.Values["controller"].ToString() == "Dashboard" ? "active" : "")">
                <i class="fas fa-tachometer-alt"></i>
                <span>Dashboard</span>
            </a>
            <a href="@Url.RouteUrl("LecturerActivities")"
                class="nav-item @(ViewContext.RouteData.Values["controller"].ToString() == "Activities" ? "active" : "")">
                <i class="fas fa-calendar-alt"></i>
                <span>Quản lý hoạt động</span>
            </a>
        </div>

        <div class="sidebar-footer">
            <a href="#" onclick="document.getElementById('logoutForm').submit(); return false;" class="btn-logout">
                <i class="fas fa-sign-out-alt"></i>
                <span>Đăng xuất</span>
            </a>
            <form id="logoutForm" action="@Url.Action("Logout", "Account")" method="post" style="display:none;">
                @Html.AntiForgeryToken()
            </form>
        </div>
    </div>

    <!-- Main Content -->
    <div class="admin-content" id="adminContent">
        @if (TempData["SuccessMessage"] != null)
        {
            <div class="alert alert-success">
                <i class="fas fa-check-circle"></i>
                <span>@TempData["SuccessMessage"]</span>
            </div>
        }
        @if (TempData["ErrorMessage"] != null)
        {
            <div class="alert alert-danger">
                <i class="fas fa-exclamation-circle"></i>
                <span>@TempData["ErrorMessage"]</span>
            </div>
        }
        @if (ViewBag.ErrorMessage != null)
        {
            <div class="alert alert-danger">
                <i class="fas fa-exclamation-circle"></i>
                <span>@ViewBag.ErrorMessage</span>
            </div>
        }

        @RenderBody()
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            // Sidebar Toggle
            $('#sidebarToggle').click(function () {
                $('#adminSidebar').toggleClass('collapsed');
                $('#adminHeader').toggleClass('sidebar-collapsed');
                $('#adminContent').toggleClass('sidebar-collapsed');
                
                // Save state to localStorage
                const isCollapsed = $('#adminSidebar').hasClass('collapsed');
                localStorage.setItem('lecturerSidebarCollapsed', isCollapsed);
            });

            // Restore state from localStorage
            const isCollapsed = localStorage.getItem('lecturerSidebarCollapsed') === 'true';
            if (isCollapsed) {
                $('#adminSidebar').addClass('collapsed');
                $('#adminHeader').addClass('sidebar-collapsed');
                $('#adminContent').addClass('sidebar-collapsed');
            }
        });
    </script>
    @RenderSection("scripts", required: false)
</body>

</html>
